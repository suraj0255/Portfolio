@inherits LayoutComponentBase
@inject NavigationManager Navigation

<div class="component-container portfolio-app">
    <NavMenu />

    <main>
        <article class="portfolio-main">
            @Body
        </article>
    </main>
</div>

<style>
    body {
        margin: 0;
        padding: 0;
        background-color: #1e1e1e;
        font-family: 'Inter', Arial, sans-serif;
        min-height: 100vh;
        color: #f5f5f5;
    }

    .portfolio-main {
        display: flex;
        flex-direction: column;
        gap: 2rem;
        align-items: stretch;
        justify-content: flex-start;
        width: 100%;
        flex: 1 1 auto;
        padding: 2rem;
        position: relative;
        z-index: 1;
    }
</style>
